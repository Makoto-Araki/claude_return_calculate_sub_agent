# タスク一覧 - エラーレスポンスの整形 (error-handling)

- [x] `apps/main.py` に `RequestValidationError` の例外ハンドラを実装する(非有限のfloatをエラー詳細内で文字列化して `422` を返す) (Req 1, 2, 3)
- [x] `apps/main.py` に、値を再帰的に走査して非有限のfloatを文字列化するヘルパー関数を実装する (Req 2, 3)
- [x] `tests/unit/test_error_handling.py` にユニットテストコードを実装する(4エンドポイントすべてを `parametrize` で網羅する) (Req 1, 2, 3, 4, 5)
  - [x] 異常系テスト: `a`/`b` が `NaN`・`Infinity`・`-Infinity` の場合に、500ではなく422が返る (Req 1)
  - [x] 異常系テスト: `a`/`b` が `1e400`・`-1e400`(floatの範囲を超える数値)の場合に、500ではなく422が返る (Req 1)
  - [x] 異常系テスト: 非有限のfloatがネストした値(配列・オブジェクトの要素)、リクエストボディ全体、他の検証エラー(`b` の欠落)との同時発生の場合も422が返る (Req 2)
  - [x] 異常系テスト: 422のレスポンスが標準の形式(`detail` がリスト、`type`・`loc`・`msg`・`input` を持つ)であり、非有限のfloatが `"nan"`・`"inf"`・`"-inf"` に置換され、それ以外の値が変更されていない (Req 3)
  - [x] 異常系テスト: 非有限のfloatを含まない不正なリクエスト(`0`・負数・小数・数値でない値・欠落)の422が、標準どおり `input` の値を含んだまま変わらない (Req 4)
  - [x] 正常系テスト: 有効なリクエストのレスポンスが従来どおりである(既存の各演算のテスト全件がGreenのまま) (Req 5)
